$(document).ready( function() {

});

function checkExtraPrice(start_price) {
  switch (start_price) {
    case: < 22
      return start_price
    case: < 150
      start_price += 
      return 
  }
}